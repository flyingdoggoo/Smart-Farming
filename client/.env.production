# Production environment variables for Vite React frontend
# Thay k23bkdn.io.vn bằng domain thật của bạn
#
# Nếu dùng Vite dev server + proxy (Cách 1) → không cần file này
# Nếu build static (Cách 2) → uncomment dòng dưới và chạy: npm run build

# VITE_API_URL=https://api.smartfarm.k23bkdn.io.vn
